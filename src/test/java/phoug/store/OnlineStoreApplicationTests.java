package phoug.store;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class OnlineStoreApplicationTests {

	@Test
	void contextLoads() {
		// This test is intentionally left empty to verify that the
		// Spring application context loads successfully.
		// If the context fails to load, the test will fail automatically.
	}

}
